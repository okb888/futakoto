# Claude Code handoff for futakoto

このプロジェクトの継続実装では、まず必ず以下を読むこと。

1. `AGENTS.md`
   - 現在の進捗
   - 2026-05-06 までに実施した変更
   - AI認証エラーの解決内容
   - 相談タブ・お気に入り・振り返りフィルタの仕様
   - 次にやるべき最優先タスク
2. `.design/system.md`
   - UIの色・余白・タイポグラフィ・トーンの唯一の基準

## 重要

- `AGENTS.md` がこのリポジトリの最新ハンドオフ本体。
- UIを触る前に `.design/system.md` を読む。
- 返信機能は採用しない。
- AI添削は単純要約にしない。必ず意図・葛藤・自己認識を残す。
- お気に入りは自分だけに見える印。相手に通知・表示しない。
- 2026-05-06時点の最優先タスクは `lib/firebase.ts` の `getReactNativePersistence` 型エラー修正。
