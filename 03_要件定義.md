---
type: project
created: 2026-05-05
status: 検討中
---

# ふたこと 要件定義（v1.0）

決めないと実装でブレる論点を潰すためのドキュメント。完璧な仕様書ではない。

---

## 1. 主要UXフロー

### 1-1. 初回起動フロー（新規ユーザー）

```
アプリ起動
  ↓
ログイン画面
  ↓
[新規登録] → メールアドレス + パスワード
  ↓
（オンボーディング画面 ← 作るか要決定）
  ↓
ホーム画面（投稿0件・「最初の気持ちを記録しよう」）
```

**決めるべきこと:**
- [ ] オンボーディング画面を作るか？（アプリの使い方説明）
- [ ] 表示名（ニックネーム）の入力を求めるか？（今はメアドの@より前を使ってる）

### 1-2. ペアリングフロー

```
[A側] 設定 → 招待コードをコピー → 送信（LINE等で）
[B側] アプリインストール → 新規登録 → 設定 → コード貼り付け → 繋がる
  ↓
両者のホームに「❤ ◯◯さんと繋がっています」
```

**現状の懸念:**
- 招待コードがUID（28文字）で長い → 短縮した6桁コードに変えるか要検討
- LINE等で送るときの文面テンプレ（自動生成）があるとUX良い

### 1-3. 毎日使うフロー（メイン）

```
アプリ起動
  ↓
ホーム画面（最新の投稿一覧）
  ↓
FAB「＋」タップ
  ↓
投稿画面
  ├ 絵文字（5段階）選択 ← 必須
  ├ 一言メモ ← 任意
  └ 共有範囲（ふたりだけ / 自分だけ）← デフォルト「ふたりだけ」
  ↓
保存 → ホームに戻る → 自分の投稿が一番上に
```

### 1-4. ペアリング解除フロー

```
設定 → ❤連携中の表示の「解除」タップ
  ↓
確認ダイアログ「解除しますか？」
  ↓
解除 → ホームから相手の投稿が消える
```

**決めるべきこと:**
- [ ] 解除後、過去に共有された投稿は **見える** ままにするか **消す** か
  - 案A: 自分が書いた投稿は自分の側に残る、相手のは見えなくなる（推奨）
  - 案B: 完全に切り離す（一方的な解除でも双方向）

### 1-5. アカウント削除フロー（App Store要件）

```
設定 → アカウント削除
  ↓
パスワード再入力（誤操作防止）
  ↓
全データ削除確認ダイアログ
  ↓
Firestoreから自分のドキュメント・投稿を削除 + Auth削除
  ↓
ログイン画面に戻る
```

---

## 2. エッジケース・仕様確認事項

### 2-1. ペアリング系

| ケース | 仕様案 |
|---|---|
| 自分のコードを自分で入力 | エラー「自分のコードは使えません」（実装済） |
| 存在しないコードを入力 | エラー「招待コードが見つかりません」（実装済） |
| 既にペアリング済みの人のコードを入力 | エラー「相手はすでに別のパートナーと繋がっています」（実装済） |
| 自分が既にペアリング済みなのに別のコードを入力 | 現状未チェック → エラーにすべき |
| パートナーが先にアカウント削除した | パートナーUIDは残るが、データが取れない → ホームでエラーにせず無視 |

### 2-2. 投稿系

| 論点 | 案A | 案B | 推奨 |
|---|---|---|---|
| 投稿の編集 | 不可 | 可（ただし共有後は履歴残る） | **不可**（シンプル・誤操作防止） |
| 投稿の削除 | 不可 | 可（自分のみ） | **可**（自分の投稿のみ） |
| 「自分だけ」を後から「ふたりだけ」に変更 | 不可 | 可 | **可**（README の「下書き」概念に対応） |
| 投稿時刻のタイムゾーン | サーバー時刻 | 端末時刻 | **サーバー時刻**（serverTimestamp） |

### 2-3. 表示・通知系

| 論点 | 仕様案 |
|---|---|
| 既読表示 | しない（プレッシャーになる）|
| 相手が投稿したときの通知 | する（W5以降で実装）|
| 自分が投稿しても通知が飛ぶ？ | 自分には飛ばない |
| ホーム画面に表示する件数 | 直近20件（実装済）|
| 過去全件閲覧 | タイムライン画面で月別表示（W4） |

### 2-4. データ・容量系

| 論点 | 仕様案 |
|---|---|
| 1投稿のメモ最大文字数 | 500文字（後で拡張可） |
| 1日に投稿できる回数 | 無制限 |
| 履歴の保存期間 | 無制限 |
| オフライン対応 | しない（v1.0では）|

---

## 3. App Store要件チェックリスト（必須）

### 3-1. リリース前に絶対やること

- [ ] **プライバシーポリシー** を作成・URLを App Store Connect に登録
  - 取得するデータ: メールアドレス、投稿内容、UID
  - 使用目的: アプリ機能提供のみ
  - 第三者共有: なし
- [ ] **利用規約** を作成（推奨）
- [ ] **アカウント削除機能** をアプリ内に実装（5.1.1.v 必須）
- [ ] **データ削除機能** = アカウント削除時に全投稿を消す
- [ ] **App Tracking Transparency** ダイアログ（広告を出す場合のみ・W5）
- [ ] **年齢レーティング**: 17+ 推奨（恋愛・関係性カテゴリ）
- [ ] **App Store Connect の "Sign in with Apple" 設定**: メール+パスワードのみなら不要

### 3-2. やらないとリジェクトされやすい項目

- [ ] テスター用のテストアカウント情報を提供（App Store Connect の Review 用）
- [ ] 招待コード形式のテスト手順を明記
- [ ] アプリ内の「ヘルプ」「お問い合わせ」リンク
- [ ] 不適切な投稿の通報機能（カップルアプリは1対1なので不要だが、念のため）

### 3-3. ガイドラインで気をつけること

- **4.5.4 ピアSNS要件**: 1対1専用なので影響なし
- **5.1.1 ユーザー登録**: 退会機能必須（上記）
- **3.1.2 サブスクリプション**: 月額¥500プラン → サブスク機能で実装が必要

---

## 4. 各画面の最終仕様

### 画面1: ホーム

| 要素 | 仕様 |
|---|---|
| ヘッダー | アプリ名「ふたこと」+ ペアリング状態（連携中なら相手の名前） |
| メインコンテンツ | 自分+相手（共有のみ）の投稿を時系列で表示 |
| 投稿カード | 絵文字・日時・メモ・共有範囲アイコン |
| FAB | 右下「＋」→ 投稿画面 |
| ナビ | （タブで設定・振り返りへ） |

### 画面2: 投稿

| 要素 | 仕様 |
|---|---|
| 気分（必須） | 5段階絵文字 |
| メモ（任意） | 500文字以内 |
| 共有範囲 | ふたりだけ / 自分だけ |
| 保存ボタン | 気分が選ばれていないと無効 |

### 画面3: 振り返り（W4で実装）

| 要素 | 仕様 |
|---|---|
| カレンダー | 月単位・各日に気分カラー |
| 日付タップ | その日の投稿を下にリスト表示 |
| パートナー比較 | 自分とパートナーの気分カラーを並列表示 |
| 月の傾向 | 「今月の気分平均」を簡易表示 |

### 画面4: タイムライン（v1.1で検討。MVP優先度低）

過去全件を月別グルーピングで見る画面。ホームで直近20件を見られればMVPとしては十分なので後回し可。

### 画面5: 設定

| 要素 | 仕様 |
|---|---|
| 自分の招待コード | コピーボタン付き |
| パートナー連携 | 連携状態 / コード入力 / 解除 |
| 通知設定 | ON/OFF（W5で実装） |
| アカウント | ログアウト / **アカウント削除** |
| 規約 | 利用規約 / プライバシーポリシー リンク |
| バージョン情報 | アプリバージョン |

---

## 5. 確定事項（2026-05-05 決定）

| # | 論点 | 決定 |
|---|---|---|
| 1 | 招待コードの形式 | **6桁の英数字コード**を生成（users ドキュメントに `inviteCode` 保存） |
| 2 | オンボーディング画面 | **作らない**。ホーム画面の空状態でヒントを出す |
| 3 | 表示名 | **ユーザー入力**（プロフィール画面で `displayName` を編集可能） |
| 4 | 投稿の削除 | **可**（自分の投稿のみ・確認ダイアログあり） |
| 5 | 「自分だけ」→「ふたりだけ」変更 | **可**（投稿カードからvisibility切替） |

---

## 6. AI統合（Gemini）への対応設計

W6で Gemini API を本格導入予定。基本機能完成後にスムーズに統合できるよう、**今のデータ設計と実装の段階で布石を打つ**。

### 6-1. 想定するAI機能

| 機能 | 用途 | 入力 | 出力 |
|---|---|---|---|
| **AIリライト** | 自分のモヤモヤ→伝わる言葉に変換 | 下書きテキスト | 改善案2-3パターン |
| **意図の汲み取り** | 相手の投稿を読んでも意図がわからないとき | 相手の投稿（共有のみ） | 「相手はこう感じている可能性」3案 |
| **月次要約** | 今月の自分・夫婦の気分傾向 | 直近30日の投稿 | 自然文サマリ + 共感的コメント |
| **会話のきっかけ提案** | 何を話せばいいかわからないとき | 直近の投稿コンテキスト | 「こう聞いてみては？」3案 |
| **AI壁打ち** | 自分の気持ちを整理 | 自由テキスト | 対話形式で深掘り |

### 6-2. アーキテクチャ方針

```
[アプリ] → [Cloud Functions（プロキシ）] → [Gemini API]
                ↓ 結果キャッシュ
            [Firestore]
```

- **APIキーはアプリに置かない**（Cloud Functions の環境変数）
- Cloud Functions で認証チェック・レート制限・キャッシュ
- 同じ入力には同じ出力を返す（コスト削減）

### 6-3. データモデルの拡張余地（今から入れておく）

**`entries` ドキュメントに任意フィールド追加可能な設計**:

```typescript
interface Entry {
  // 既存
  uid: string;
  mood: number;
  memo: string;
  visibility: Visibility;
  createdAt: Timestamp;

  // 将来追加（v1.1〜）
  aiSummary?: string;      // AIが生成した要約
  aiTags?: string[];       // AI抽出の感情タグ（怒り・寂しさ等）
  aiRewriteOf?: string;    // 元の投稿IDがあればAIリライト由来
  editedAt?: Timestamp;    // 編集された場合
  deletedAt?: Timestamp;   // 論理削除（物理削除でも可）
}
```

**`users` ドキュメントに将来のAI使用量管理**:

```typescript
interface UserProfile {
  // 既存
  uid: string;
  email: string;
  partnerUid?: string;
  inviteCode: string;     // 6桁コード
  displayName: string;    // ユーザー入力

  // 将来
  aiCreditsUsed?: number;  // 月間AI使用量（課金プラン管理用）
  aiCreditsResetAt?: Timestamp;
  isPremium?: boolean;    // 月額¥500プランフラグ
}
```

### 6-4. 今の実装で配慮すること（W4以降）

- [ ] 投稿取得関数 `getRecentEntries(uid, count)` は**任意件数取得可能**に → AI入力用に直近30件などまとめて取れる構造（既に実装済）
- [ ] 投稿に**個別IDを必ず付与**（`addDoc` の戻り値ID使用） → AI処理結果と紐付けるため（既に実装済）
- [ ] フロントエンドのAI機能呼び出しは**抽象化**（`lib/ai.ts` に統一）→ 後でCloud Functionsに置き換える
- [ ] プロフィール（displayName）はAIプロンプトで「相手の名前」として使う → 今からフィールドだけ用意

### 6-5. プライバシー設計

- AI処理対象: **自分の投稿のみ**がデフォルト
- 相手の共有投稿をAIに渡すのは**ユーザーが明示的にボタンを押した時だけ**
- AI処理結果は**自分のドキュメントだけに保存**（相手側には保存しない）
- 利用規約に「投稿内容をAI処理する場合がある」旨明記
- Gemini APIへの送信ログは Cloud Functions で **保持しない**（透過プロキシ）

### 6-6. コスト見積もり（参考）

- Gemini 2.5 Flash: 入力 $0.10/1M tokens、出力 $0.40/1M tokens
- 1リクエストあたり平均: 入力500 tokens / 出力300 tokens ≒ $0.00017 ≒ 0.025円
- 月額¥500プランの利益率を保つなら、**1ユーザー月間1000回まで**で十分
- 将来的に課金ユーザーが100人なら月間コスト ¥2,500程度

---

## 7. 後で決めればいいこと

- 通知の細かい挙動（W5）
- 課金プランの境界線詳細（W5）
- AIプロンプトの細かい調整（W6）
- App Store審査用の説明文（W7）

---

## 8. 次に作るドキュメント候補

- `04_プライバシーポリシー.md` ← W7前に必要（AI処理を含む内容で）
- `05_利用規約.md` ← W7前に必要
- `06_App_Store_提出資料.md` ← W7
